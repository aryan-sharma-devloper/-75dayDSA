class Solution {
    public int missingNumber(int[] nums) {
       int renum = 0 ;
       int brnum = 0 ;
        for( int i = 0; i <nums.length; i++){
            renum += i ;
            brnum += nums[i];
        }
        renum +=nums.length;
        return renum - brnum ;
    }
}
