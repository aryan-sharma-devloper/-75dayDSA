class Solution{
  public int maximumsub(int []arr){
    int curr = arr[0];
    int max = arr[0];
        for(int i = 0 ; i < arr.length; i++){
          curr = Math.max(arr[i],curr+arr[i]); // kadana's algo 
          max = Math.max(curr, max);
          }
    return max;
  }
}
